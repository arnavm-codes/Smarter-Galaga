// v1: flat baseline fire rate. K/D-band-driven scaling (see design doc) comes later,
// once the prediction engine is integrated — kept flat here on purpose.
export const BASE_ENEMY_FIRE_INTERVAL = 1.1; // seconds between enemy shots
